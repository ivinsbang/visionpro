# Infrastructure

Reserved for deployable configuration when an approved service needs hosting.
No cloud provider, infrastructure tool, environment, or resource is selected or
created. The native foundation uses local preview status and needs no backend.

Add environment definitions and deployment automation alongside the actual
service design. Keep credentials and infrastructure state out of Git. Document
deployments and recovery in `docs/operations`.
